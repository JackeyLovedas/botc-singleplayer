# Phase 3 Slice 2B20B-P2F1R-D1.5 — Routing/Coverage Baseline Alignment Design Correction Round 2

## 1. Control and authority

```text
sliceId=2B20B-P2F1R-D1.5
parentDesign=docs/architecture/2B20B-P2F1R-D1.5-routing-coverage-baseline-alignment-design-round-1.md
parentDesignSha256=0b9c8af1bc58824581ad613b483143c961ba46afd7921d956a93be95bc7e3e98
correctionRound1=docs/architecture/2B20B-P2F1R-D1.5-routing-coverage-baseline-alignment-design-correction-round-1.md
correctionRound1Sha256=c5d51f38b298a13731ba96c201785d9825569df0f3cd2c4daa11a0d5bb55dfdd
designCorrectionUsed=2/2
designCorrectionRemaining=0
implementationRepairUsed=0/2
implementationAuthorized=false
correctionScope=D15-DC1-F01_COVERAGE_ARTIFACT_GRAPH_AND_EXACT_T1_MATERIALIZATION_NOT_CLOSED
```

This document supersedes Correction Round 1 only where required to close `D15-DC1-F01`. All other parent and Correction Round 1 contracts remain frozen, including the legal Governance V1.1 vocabulary, baseline lifecycle states, complete tuple identity/classification semantics, routing topology, two-stage S/P history, file allowlists, zero product/test/rule change, and D2 stop. Rule evidence remains `RULE_READY`, SHA-256 `9d4b9b96bfbe47e411dd034b39687e16153665174da5436a117d633b8f738662`, with `involvedRoles=[]`; the role coverage matrix remains unchanged.

The exact inspected design baseline is clean branch `phase-3/2b20b-p2f1r-d1-5-routing-coverage-baseline-alignment` at `c2c617d505facb26723cb39d4f1f23f29f3e111d`. GitHub `main` remains `0dc046aa62b3a72cbd97d99808e0932bf408a09c`, open PR count is zero, and no D1.5 Hosted CI or publication exists. These observations do not authorize implementation.

## 2. Scope and non-goals

This final correction freezes exactly:

1. the five-node coverage artifact graph;
2. exact persisted schemas and acyclic hash bindings;
3. the producer and child-verifier CLI/materialization protocols;
4. deterministic candidate-JSON-to-profile-entry rendering;
5. the bounded T1 verifier contract;
6. a component-level `<=360` net-added-line budget; and
7. the complete negative matrix requested by the prior review.

It does not design or implement coverage intelligence, a dynamic artifact graph, a runtime dependency system, a runtime profile authority, a dynamic selector, a permanent migration framework, Hosted CI, publication, D2, BOTC rules, product behavior, tests, C/C1/A/B, event definitions, replay, state, or role behavior. It does not run coverage or modify a workflow.

## 3. Exact five-node Coverage Artifact Graph V1

### 3.1 Closed graph identity

The graph contains exactly five nodes and no others:

```text
SOURCE_HEAD_S
OLD_PROFILE
DELTA_RECORD
ACCEPTED_1712_PROFILE
PROFILE_CHILD_P
```

The persisted graph manifest top-level keys are exact and ordered:

```text
schemaVersion,nodes,profileCandidateJsonSha256,sourceDelta,profileDelta,inventorySha256,tupleSha256,result
```

`schemaVersion="botc-d1-5-coverage-artifact-graph-v1"`. `nodes` is a dense array of exactly five exact records in the topological order above. Every node record has exactly the ordered keys:

```text
id,sha,count,relation
```

`sha` is a lowercase hexadecimal string. It is 40 characters for the Git commit nodes `SOURCE_HEAD_S` and `PROFILE_CHILD_P`; it is 64 characters and SHA-256 over the canonical bytes defined below for the other three nodes. `relation` is a dense array of exact records with ordered keys `kind,from`; an empty relation is `[]`. Unknown node IDs, relation kinds, extra nodes, duplicate nodes, self edges, forward references, unresolved references, or cycles fail closed.

### 3.2 Exact nodes, counts, and relations

`SOURCE_HEAD_S`:

```text
id="SOURCE_HEAD_S"
sha=the exact Stage-S Git commit OID
count={"sourceCount":69,"testCount":1712,"groupCount":11}
relation=[]
```

`OLD_PROFILE`:

```text
id="OLD_PROFILE"
sha="db4e659874a33b83c97bddeec5ed84f07deaacfba249891dbb22fab03ef813dc"
count={"sourceCount":63,"testCount":1572,"groupCount":11}
relation=[]
```

The `OLD_PROFILE.sha` is the existing canonical object-byte SHA for `phase-3-slice-2b20a-4d576e2-final-restoration-v1`; its frozen object byte count remains 2930.

`DELTA_RECORD`:

```text
id="DELTA_RECORD"
sha=SHA256(canonical DELTA_RECORD bytes)
count={"sourceAdded":6,"sourceRemoved":0,"testAdded":140,"testRemoved":0,"removedIdentity":0,"missingIdentity":0,"unexpectedIdentity":0}
relation=[
  {"kind":"COMPARES_OLD_PROFILE","from":"OLD_PROFILE"},
  {"kind":"COMPARES_SOURCE_HEAD","from":"SOURCE_HEAD_S"}
]
```

`ACCEPTED_1712_PROFILE`:

```text
id="ACCEPTED_1712_PROFILE"
sha=SHA256(exact rendered Object.freeze profile-entry bytes)
count={"sourceCount":69,"testCount":1712,"groupCount":11}
relation=[
  {"kind":"PROMOTES_FROM","from":"OLD_PROFILE"},
  {"kind":"BINDS_SOURCE","from":"SOURCE_HEAD_S"},
  {"kind":"BINDS_DELTA","from":"DELTA_RECORD"}
]
```

`PROFILE_CHILD_P`:

```text
id="PROFILE_CHILD_P"
sha=the exact Stage-P Git commit OID
count={"parentCount":1,"profileEntryAdded":1,"selectorReplacementCount":1}
relation=[
  {"kind":"DIRECT_PARENT","from":"SOURCE_HEAD_S"},
  {"kind":"MATERIALIZES","from":"ACCEPTED_1712_PROFILE"}
]
```

The graph is a DAG. Its only roots are `SOURCE_HEAD_S` and `OLD_PROFILE`. `DELTA_RECORD` depends only on the two roots; `ACCEPTED_1712_PROFILE` depends only on the roots and delta; `PROFILE_CHILD_P` depends only on S and the rendered profile. `ACCEPTED_1712_PROFILE` must not contain, hash, derive, or otherwise bind `PROFILE_CHILD_P.sha`.

### 3.3 Hash-cycle exclusion

`DELTA_RECORD` binds S, the old profile identity/SHA, inventory SHA, tuple universes, and classifications; it does not bind the profile-entry SHA, P, or graph-manifest SHA. The profile candidate binds S, the inventory SHA, tuple SHA, and delta-record SHA; it does not bind its own rendered SHA, P, or graph-manifest SHA. P contains the rendered profile and reviewed documentation but no P SHA. Only after P exists does the child verifier write the graph manifest outside both worktrees. The graph manifest is not a graph node and no graph node hashes the manifest. Therefore the hash relation is acyclic and has no self-reference.

## 4. Canonical byte contract shared by persisted artifacts

`DELTA_RECORD`, profile candidate JSON, and the final graph manifest use their exact schema order, JSON strings and numbers without coercion, two-space indentation, UTF-8, LF line endings, no BOM, and exactly one terminal LF. Objects reject missing, extra, or reordered keys. Arrays must be dense. Values with accessors, symbols, proxies, revoked proxies, cycles, non-plain prototypes, sparse slots, unsafe integers, non-finite numbers, or implicit conversions fail before hashing. Repository paths are relative POSIX paths. Sorting uses the existing ordinal comparator over UTF-8/code-unit identity bytes; locale APIs and insertion order are forbidden. SHA-256 is lowercase 64-character hex over exact bytes. There is no ambiguous optional terminal newline.

## 5. DELTA_RECORD exact schema and inline tuple authority

### 5.1 Top-level schema

`delta-record.json` has exactly these ordered top-level keys:

```text
schemaVersion,sourceHead,oldProfileId,oldProfileSha256,oldInventorySha256,candidateInventorySha256,sourceDelta,testDelta,tupleBinding,tupleSha256,classificationCounts,lossCounts,result
```

Exact constants are:

```text
schemaVersion="botc-d1-5-delta-record-v1"
oldProfileId="phase-3-slice-2b20a-4d576e2-final-restoration-v1"
oldProfileSha256="db4e659874a33b83c97bddeec5ed84f07deaacfba249891dbb22fab03ef813dc"
oldInventorySha256="58bd4b6959c1f234ac74b90b1188cccf08ebeb5bdfaecdebd900e49d69a0e1b8"
candidateInventorySha256="540e2f2a92132ad43b299e95c6515d2349c514f66db4e1054b6eb0f9474cf7d2"
```

`sourceHead` is exactly `SOURCE_HEAD_S`. `sourceDelta` has exact ordered keys `from,to,added,removed,unchanged,changed,addedPaths,changedPath` and exact values `63,69,6,0,62,1`, the six frozen added production paths in ordinal order, and `packages/domain-core/src/index.ts`. `testDelta` has exact ordered keys `from,to,added,removed,intersection` and exact values `1572,1712,140,0,1572`.

`tupleBinding` has exact ordered keys:

```text
schemaVersion,oldUniverse,sourceUniverse,classifications
```

with `schemaVersion="botc-d1-5-inline-complete-tuple-binding-v1"`. `tupleSha256` is SHA-256 of `tupleBinding` reserialized alone under section 4. No external `completeTupleMapPath`, sibling tuple-map file, implicit coverage summary, or zero-hit-only map is permitted.

### 5.2 Complete inline universes

`oldUniverse` and `sourceUniverse` each have exactly these ordered keys:

```text
sourceCount,statementTuples,functionTuples,lineTuples,branchArmTuples,completeTupleCount,completeTupleSha256
```

All four tuple arrays are complete maps, including `ZERO` and `POSITIVE` hit states, not only uncovered tuples. Tuple forms remain exactly:

```text
["STATEMENT",path,startLine,startColumn,endLine,endColumn,hitState]
["FUNCTION",path,name,declStartLine,declStartColumn,declEndLine,declEndColumn,locStartLine,locStartColumn,locEndLine,locEndColumn,hitState]
["LINE",path,line,hitState]
["BRANCH_ARM",path,branchType,branchStartLine,branchStartColumn,branchEndLine,branchEndColumn,armIndex,armStartLine,armStartColumn,armEndLine,armEndColumn,hitState]
```

`hitState` is exactly `ZERO|POSITIVE`; semantic identity is the same tuple without its final hit-state value. Each kind is sorted by semantic identity bytes, and kinds occur in the exact order statement, function, line, branch arm. `completeTupleCount` equals the sum of all four array lengths. `completeTupleSha256` hashes the LF-terminated compact JSON tuple lines in that same kind/order sequence. The old universe has 63 sources; the source universe has 69. Missing maps, duplicate semantic identities, branch-arm count mismatch, incomplete source coverage, invalid hit counts, or a stored SHA mismatch fail closed.

`classifications` is a dense array over the exact set union of old and source semantic identities. Each record has ordered keys `classification,kind,identity,oldHitState,newHitState,sourcePath,reason`. It retains the frozen six-token union and precedence from Correction Round 1:

```text
UNCHANGED_IDENTITY
CHANGED_IDENTITY
EXPECTED_ADDITION
REMOVED_IDENTITY
MISSING_IDENTITY
UNEXPECTED_IDENTITY
```

`classificationCounts` has those six keys in that order. `lossCounts` has exact ordered keys `stablePositiveBecameZero,modifiedPositiveBecameZero,unexplainedPriorPositiveLoss,unclassifiedIdentity,contradictoryIdentity`. `result="PASS"` is legal only when removed, missing, unexpected, unclassified, contradictory, and all positive-to-zero loss counts are zero. `CHANGED_IDENTITY` is restricted to the sole modified index file; `EXPECTED_ADDITION` is restricted to the six frozen added paths. Relocation remains forbidden. Equal-count delete/add substitution, line movement, same function name, or text similarity never discharges an old positive identity.

## 6. Unique Stage-S producer contract

### 6.1 Exact command and seven operands

The only producer is a closed mode in `scripts/verify-coverage-obligations.mjs`:

```text
node scripts/verify-coverage-obligations.mjs --emit-d1-5-profile-candidate SOURCE_HEAD_S OLD_WORKTREE_ROOT phase-3-slice-2b20a-4d576e2-final-restoration-v1 TEMP_CANDIDATE_JSON .vitest-coverage/segmented-global/global-manifest.json .vitest-coverage/segmented-global/coverage-output/coverage-final.json EMPTY_OUTPUT_DIRECTORY
```

After the mode token there are exactly seven operands, by ordinal:

| Ordinal | Name | Type and authority |
|---:|---|---|
| 1 | `SOURCE_HEAD_S` | exact 40-character lowercase Git OID; must equal current clean HEAD |
| 2 | `OLD_WORKTREE_ROOT` | clean detached real directory at `4d576e205cb20c37ba913b923a1cd39e8d800d18` |
| 3 | `OLD_PROFILE_ID` | exact literal `phase-3-slice-2b20a-4d576e2-final-restoration-v1` |
| 4 | `TEMP_CANDIDATE_JSON` | existing D1 persisted candidate regular file under OS temp |
| 5 | `SOURCE_GLOBAL_MANIFEST` | exact repository-relative POSIX path shown in the command |
| 6 | `SOURCE_COVERAGE_FINAL` | exact repository-relative POSIX path shown in the command |
| 7 | `EMPTY_OUTPUT_DIRECTORY` | pre-existing empty real directory under OS temp and outside both worktrees |

The old global manifest and old coverage-final input are resolved only at the same two fixed repository-relative paths under `OLD_WORKTREE_ROOT`; they are not additional operands. `process.argv.slice(2)` must therefore contain exactly eight strings including the mode. Missing, extra, reordered, aliased, prefixed, `latest`, environment-selected, or inferred operands fail with `D15_INVALID_ARGUMENTS` before any file read.

The producer verifies both Git objects are locally available, the source and old worktrees are clean and exact, every input is a real regular file beneath its declared root, the candidate is a direct OS-temp file, and the output directory is a direct OS-temp directory outside both worktrees. Directories where files are required, symlinks, junctions, devices, pipes, sockets, root escapes, mixed worktrees, and wrong siblings fail closed. It invokes no coverage command and writes no repository file.

### 6.2 Exact producer outputs

Using exclusive creation plus same-directory atomic rename, the producer creates exactly three files and no others:

```text
delta-record.json
profile-candidate.json
profile-entry.mjsfrag
```

`delta-record.json` is section 5. `profile-candidate.json` is section 7.1. `profile-entry.mjsfrag` is the exact section 7.2 renderer output. Repeating the producer with identical inputs in a second empty output directory must yield byte-identical three files and byte-identical stdout.

Success has exit code `0`, empty stderr, and exactly one LF-terminated stdout line:

```text
D1_5_PROFILE_CANDIDATE_EMITTED SOURCE_HEAD_S deltaRecordSha256 profileCandidateJsonSha256 profileEntrySha256
```

Validation failure has exit code `1`, empty stdout, and exactly one closed diagnostic token plus LF on stderr. Unexpected internal failure has exit code `2`, empty stdout, and `D15_INTERNAL_FAILURE\n`. No output may contain an absolute path, artifact body, test identity/title, environment value, stack, PID, timestamp, random value, or locale-derived text.

## 7. Profile candidate and deterministic registry renderer

### 7.1 Candidate JSON schema

`profile-candidate.json` has exact ordered keys:

```text
schemaVersion,baselineVersion,inventorySha256,tupleSha256,deltaRecordSha256,profile
```

Exact constants are `schemaVersion="botc-d1-5-profile-candidate-v1"`, `baselineVersion="ACCEPTED_1712_V1"`, and the candidate inventory SHA from section 5. `tupleSha256` and `deltaRecordSha256` must match the exact sibling `delta-record.json` content. The `profile` object has the current registry’s exact ordered top-level keys:

```text
id,sourceHead,sourceKind,topology,obligations
```

Its ID is deterministically `"phase-3-slice-2b20b-p2f1r-d1-5-" + first7(SOURCE_HEAD_S) + "-routing-coverage-v1"`; `sourceHead=SOURCE_HEAD_S`; `sourceKind="EXACT_SOURCE_SEGMENTED_COVERAGE_AUTHORITY"`.

`topology` has exact ordered keys:

```text
id,ordinaryGroups,ordinaryPhysicalBlobs,coverageGroups,coveragePhysicalBlobs,coverageLogicalGroups,semanticTests,coverageGlobalManifestSha256,coverageFinalSha256,normalizedTupleSetsSha256,fullTupleDeltaSha256,baselineVersion,inventorySha256
```

It freezes ordinary `9/11/1712`, coverage `11/12/1712`, exact current group order/counts with only `domain-core-rest=503`, the fresh source manifest/coverage hashes, `normalizedTupleSetsSha256=tupleSha256`, `fullTupleDeltaSha256=deltaRecordSha256`, `baselineVersion="ACCEPTED_1712_V1"`, and the exact candidate inventory SHA. `obligations` has exact ordered keys `sourceFiles,zeroHitStatements,zeroHitFunctions,zeroHitLines,zeroHitBranchArms`; every value has exact ordered keys `count,sha256`. Actual source-head obligation counts and hashes are implementation actual bindings derived from the validated complete source map, not design-time guesses.

The candidate lifecycle is `PENDING` until all child checks pass. The candidate JSON is evidence input to a renderer; it is never runtime profile authority and is never selected dynamically.

### 7.2 Single deterministic renderer

Only `scripts/verify-coverage-obligations.mjs` may render the candidate. It contains one slice-specific renderer from the validated candidate JSON to one `Object.freeze({...})` entry. The renderer is not generic and accepts only the exact section 7.1 schema/version.

The emitted fragment:

- begins exactly with `  Object.freeze({\n`;
- ends exactly with `  })` and has no terminal LF;
- uses two spaces per indentation level;
- emits property names in the frozen schema order;
- emits `: ` between key and value and `,\n` between siblings;
- uses `JSON.stringify` only for already-captured primitive string values;
- emits non-negative safe integers in canonical base-10 without leading zeros;
- emits every object as `Object.freeze({` and every array as `Object.freeze([`;
- emits coverage-group and physical-segment records in frozen routing order;
- emits no comment, timestamp, absolute path, computed getter, spread, trailing comma, BOM, CR, or locale-derived content.

There is no second renderer, template file, formatter, code-generation dependency, or object-to-source fallback.

### 7.3 Exact registry splice and sentinels

The registry prefix sentinel is the unique UTF-8/LF byte sequence:

```text
const APPROVED_COVERAGE_PROFILES = Object.freeze([\n
```

The registry closing sentinel is the unique sequence:

```text
\n]);\n\nfunction parseArguments(argv) {
```

At S the complete script bytes and every existing profile span are frozen. At P, the only registry splice replaces the closing sentinel with these exact bytes:

```text
,\n + profileEntryBytes + \n]);\n\nfunction parseArguments(argv) {
```

The start sentinel for the new span is the exact prefix `  Object.freeze({\n    id: ` immediately followed by the JSON-string form of the mechanically derived profile ID. The end of the span is exactly the final `  })` immediately before the registry closing sentinel. The span SHA-256 must equal `ACCEPTED_1712_PROFILE.sha`. All bytes before the inserted comma and after the closing sentinel must equal S; all existing profile object spans, including `OLD_PROFILE`, must remain byte-identical.

Round-trip verification is exact and does not evaluate untrusted source: parse the candidate JSON, validate it, render bytes, locate the unique P byte span by the two sentinels, compare the span byte-for-byte with renderer output, then compare the already-loaded runtime registry entry’s closed data fields recursively with candidate `profile`. Any duplicate sentinel, ambiguous span, altered old span, alternate formatting, extra field, or semantic-only match with different bytes fails closed. The workflow selector is one exact literal replacement from the old profile ID to the new derived ID; no fallback, prefix match, registry-order selection, or dynamic lookup exists.

## 8. Stage-P child materialization and final graph manifest

Stage P remains a direct single-parent child:

```text
parent(PROFILE_CHILD_P)=SOURCE_HEAD_S
```

At P, the exact command is:

```text
node scripts/verify-coverage-obligations.mjs --verify-d1-5-profile-child SOURCE_HEAD_S DELTA_RECORD_PATH PROFILE_CANDIDATE_JSON_PATH FINAL_GRAPH_MANIFEST_PATH
```

There are exactly four operands after the mode. The two input paths are the regular OS-temp artifacts emitted together by the S producer. `FINAL_GRAPH_MANIFEST_PATH` is a non-existing direct child of an existing empty OS-temp output directory outside both worktrees. The verifier also finds `profile-entry.mjsfrag` only as the fixed sibling of `PROFILE_CANDIDATE_JSON_PATH`; arbitrary sibling naming is forbidden.

Before writing anything, the child verifier checks: current clean HEAD is P; P has exactly one parent S; required Git objects exist; S/P allowlists and frozen trees hold; the delta and candidate are canonical and sibling-consistent; the fixed fragment exists and hashes correctly; the exact registry splice and old-profile byte preservation hold; the workflow has exactly one selector-token replacement; no selector fallback exists; and the five-node graph values/relations are exact.

It then serializes the section 3 manifest under section 4, rereads and rehashes the same-directory temporary file, and atomically renames it without replacement. Success is exit `0`, empty stderr, and exactly:

```text
D1_5_PROFILE_CHILD_VERIFIED PROFILE_CHILD_P SOURCE_HEAD_S finalGraphManifestSha256
```

Failure is exit `1`, empty stdout, and one closed diagnostic plus LF; internal failure is exit `2` and `D15_INTERNAL_FAILURE\n`. The final graph manifest is generated after P and stored outside Git, so it may bind P without self-reference. Its SHA is recorded in implementation traceability and the archived migration record, not in any graph node.

## 9. T1 materialization boundary

The T1 verifier verifies only:

1. required artifact existence as real regular files;
2. exact artifact identity, schema, sentinel, and graph-node identity;
3. SHA of delta bytes, candidate JSON bytes, rendered entry bytes, old profile bytes, S, and P;
4. `inventorySha256=540e2f2a92132ad43b299e95c6515d2349c514f66db4e1054b6eb0f9474cf7d2`;
5. complete inline `tupleSha256` and its agreement with the profile and graph;
6. source count `63 -> 69` and source delta `+6/-0`;
7. semantic test count `1572 -> 1712` and test/profile delta `+140/-0`; and
8. profile object delta `+1/-0`, direct S-to-P parentage, and one selector replacement.

The final manifest `sourceDelta` has exact ordered keys `from,to,added,removed` with values `63,69,6,0`. `profileDelta` has exact ordered keys `testFrom,testTo,testAdded,testRemoved,profileObjectsAdded,profileObjectsRemoved` with values `1572,1712,140,0,1,0`. `result="PASS"` is legal only when all listed checks pass.

T1 does not judge coverage percentage or quality, product behavior, BOTC rule correctness, C/C1 validity, event semantics, replay, authority issuance, publication, Hosted CI, or whether a future slice should proceed. No artifact in this contract becomes runtime profile authority.

## 10. Closed diagnostics and component budget

### 10.1 Closed diagnostics

Correction Round 1’s closed diagnostic union remains and adds no open strings. The materialization paths use only:

```text
D15_INVALID_ARGUMENTS
D15_SOURCE_HEAD_MISMATCH
D15_SOURCE_WORKTREE_DIRTY
D15_OLD_HEAD_MISMATCH
D15_OLD_WORKTREE_DIRTY
D15_PATH_INVALID
D15_PATH_ESCAPE
D15_PATH_SYMLINK_OR_JUNCTION
D15_INPUT_NOT_REGULAR_FILE
D15_OUTPUT_DIRECTORY_INVALID
D15_OUTPUT_COLLISION
D15_JSON_INVALID
D15_ENVELOPE_SHAPE_INVALID
D15_ENVELOPE_ORDER_INVALID
D15_INVENTORY_INVALID
D15_ROUTING_INVALID
D15_COVERAGE_INVALID
D15_ARTIFACT_HASH_MISMATCH
D15_TUPLE_MAP_INCOMPLETE
D15_TUPLE_CLASSIFICATION_INVALID
D15_OBLIGATION_LOSS
D15_PROFILE_CANDIDATE_INVALID
D15_PROFILE_PARENT_INVALID
D15_PROFILE_NOT_APPEND_ONLY
D15_SELECTOR_INVALID
D15_FROZEN_TREE_CHANGED
D15_INTERNAL_FAILURE
```

For this correction, `D15_ENVELOPE_*` applies to the candidate, delta, and final graph manifest exact records; it no longer implies separate old/source authority files. The diagnostic precedence is: argv; current/old Git identity and cleanliness; path/type/containment; canonical JSON/schema/order; candidate inventory/routing; complete tuple/delta; artifact hashes; candidate rendering/profile append; parent/frozen trees; selector; internal failure. A later check cannot mask an earlier failure.

### 10.2 Verifier LOC budget

The hard budget is the net added lines in `scripts/verify-coverage-obligations.mjs` from the exact implementation base through P, computed by `git diff --numstat` as additions minus deletions. It includes the appended profile entry and every D1.5 mode/helper. Blank lines and comments count because Git counts them. The component ceilings are disjoint and sum to exactly 360:

| Component | Maximum net added LOC |
|---|---:|
| appended frozen `ACCEPTED_1712_PROFILE` registry entry | 76 |
| exact argv parsing plus path/regular-file/worktree safety | 48 |
| candidate/delta/graph exact-schema and canonical-byte validation | 52 |
| complete tuple maps, delta classification, Git ancestry and hash binding | 78 |
| deterministic profile renderer, sentinels, byte-span and round-trip validation | 46 |
| P child checks, graph-manifest creation and reread verification | 38 |
| closed diagnostics and deterministic stdout/stderr dispatch | 22 |
| **Total** | **360** |

The estimate relies only on existing ordinal, SHA-256, coverage parsing/summary, exact-profile registry, and Node standard-library primitives already present in the script; no new helper file or dependency is allowed. A component may use fewer lines, but unused lines cannot raise another component above its stated ceiling without a fresh governance decision. Any component breach or total above 360 stops with `HUMAN_BLOCKED / RESLICE_REQUIRED`; the implementation may not compress by weakening validation, hide code in generated strings, add a sibling script, or reinterpret the budget. Test and documentation lines are outside this script budget but remain inside the existing file allowlist.

## 11. Complete negative matrix

Each test uses a real producer artifact or a one-point mutated clone, expects exit `1`, empty stdout, one exact diagnostic plus LF, no repository write, and no final manifest unless explicitly testing atomic collision. No row creates a general security or coverage-intelligence framework.

| ID | Single mutation | Expected diagnostic/result |
|---|---|---|
| N01 | producer mode missing, extra, reordered, aliased, prefixed, or not followed by exactly seven operands | `D15_INVALID_ARGUMENTS` |
| N02 | child mode missing/extra/reordered operand | `D15_INVALID_ARGUMENTS` |
| N03 | required Git object unavailable/shallow, wrong S, wrong P, P with zero/two parents | `D15_SOURCE_HEAD_MISMATCH` or `D15_PROFILE_PARENT_INVALID` |
| N04 | source/old worktree dirty or mixed-head | matching source/old diagnostic |
| N05 | input is directory, symlink, junction, device, escape, or wrong sibling | matching closed path diagnostic |
| N06 | non-JSON, truncated JSON, BOM, CRLF, or missing terminal LF | `D15_JSON_INVALID` or envelope diagnostic |
| N07 | candidate/delta/manifest missing, extra, or reordered key | `D15_ENVELOPE_SHAPE_INVALID` or `D15_ENVELOPE_ORDER_INVALID` |
| N08 | graph missing/extra/duplicate node, wrong node order, unknown relation, unresolved edge, self edge, or cycle | `D15_ENVELOPE_SHAPE_INVALID` |
| N09 | wrong schema version, S/P SHA, old profile identity/SHA, inventory SHA, tuple SHA, delta SHA, profile-entry SHA, topology count, or group count | `D15_ARTIFACT_HASH_MISMATCH`, `D15_INVENTORY_INVALID`, or `D15_ROUTING_INVALID` by precedence |
| N10 | old or source complete map omits a source/kind/tuple, duplicates an identity, or mismatches branch-arm counts | `D15_TUPLE_MAP_INCOMPLETE` |
| N11 | stable prior positive tuple becomes zero or disappears | `D15_OBLIGATION_LOSS` |
| N12 | modified `index.ts` prior positive tuple becomes zero or disappears | `D15_OBLIGATION_LOSS` |
| N13 | an added-source zero tuple is classified outside `EXPECTED_ADDITION` | `D15_TUPLE_CLASSIFICATION_INVALID` |
| N14 | delete/add equal-count substitution | loss or classification diagnostic; never netted |
| N15 | location-only relocation | `D15_OBLIGATION_LOSS`; never paired |
| N16 | duplicate, contradictory, unexpected, or unclassified union member | `D15_TUPLE_CLASSIFICATION_INVALID` |
| N17 | incomplete old/source full-hit map disguised as a zero-only summary | `D15_TUPLE_MAP_INCOMPLETE` |
| N18 | candidate baseline marked legacy/accepted, wrong count, or wrong serialized SHA | `D15_INVENTORY_INVALID` |
| N19 | candidate JSON altered while rendered entry remains old, or renderer output differs by one byte | `D15_PROFILE_CANDIDATE_INVALID` or `D15_ARTIFACT_HASH_MISMATCH` |
| N20 | old profile/object/prefix/suffix byte mutation or more than one appended profile | `D15_PROFILE_NOT_APPEND_ONLY` |
| N21 | profile contains P/self-reference, P parent is not S, or manifest relation is reversed | `D15_PROFILE_PARENT_INVALID` |
| N22 | selector missing, old, partial, inferred, duplicated, or fallback-enabled | `D15_SELECTOR_INVALID` |
| N23 | final manifest output already exists or atomic reread hash differs | `D15_OUTPUT_COLLISION` or `D15_ARTIFACT_HASH_MISMATCH` |
| N24 | repeated identical producer input produces different bytes/stdout, extra stdout/stderr, or any absolute-path leak | evidence fails closed; no profile or manifest is accepted |

The implementation also performs one positive round trip for each canonical artifact, both clean Git roots, the exact five-node DAG, exact S/P relation, exact source/test/profile deltas, and deterministic duplicate producer execution.

## 12. Traceability, allowlist, and lifecycle

### 12.1 Governance V1.1

Correction Round 1’s nine-field criterion ledger remains authoritative and unchanged. Its census remains exactly:

```text
activeCriteria=8
uniquePrimaryIdentities=8
duplicatePrimaryAssignments=0
borrowedPrimaryAssignments=0
R1=[]
R2=[]
R3=[]
R4=[D1.5-C01,D1.5-C02,D1.5-C03,D1.5-C04,D1.5-C05,D1.5-C06,D1.5-C07,D1.5-C08]
STRUCTURAL_VALIDATION=8
allOtherLegalPrimaryLayers=0
MIXED=0
MULTI_LAYER=0
```

`D1.5-C04`, `D1.5-C05`, and `D1.5-C06` use the exact S/P relation, renderer/span round trip, five-node graph, inline tuple binding, and T1 checks in this correction. Design-time supporting authorities remain planned descriptors, not final `SUP-*` IDs. The D1 candidate remains `CANDIDATE`, not `LEGACY` or `ACCEPTED`. `MechanismMatch` cannot be marked `PASS` before real implementation evidence exists.

### 12.2 Allowlist and workflow boundary

The parent/Correction Round 1 implementation allowlists remain unchanged. No new script, dependency, test, test identity, ownership asset, routing group, Vitest project, generic manifest library, workflow, coverage runner, or permanent verifier is added by this correction. This design round changes documentation only. It runs no coverage, local gate, Hosted CI, ownership publication, push, or PR action.

A later separately authorized Stage P may still perform only the previously frozen one-line exact profile selector token replacement in `.github/workflows/ci.yml`; this correction neither performs nor broadens it. D2 remains frozen and cannot consume a candidate or pending profile.

### 12.3 Asset lifecycle

The closed lifecycle is:

| Lifecycle | Exact assets |
|---|---|
| `KEEP` | only the final separate `ACCEPTED_1712_V1` baseline (including its selected registry profile entry) and the minimum final five-node graph manifest needed to verify its hashes and S/P binding |
| `ARCHIVE` | immutable historical 1572 baseline/profile, immutable D1 candidate evidence, D1.5 migration record, final delta/profile audit, traceability, and reviews |
| `DELETE_AFTER_D1_5` | D1.5-only producer/child-verifier modes after separately reviewed cleanup, temporary candidate/delta/fragment/manifest copies after required archival, detached worktrees, coverage directories, and scratch output |

There is no default `KEEP` for migration tools and no permanent migration or coverage-intelligence framework. Historical profiles are never overwritten. Cleanup is forbidden until hashes are archived and reverified and requires a separately reviewed cleanup; it does not authorize history rewriting.

## 13. Acceptance checks for a future separately authorized implementation

1. Reverify exact clean design-review HEAD, rule evidence, role matrix, Git objects, and no open conflicting slice PR.
2. Materialize Stage S only within the inherited allowlist; run the exact ownership candidate validation and routing gates.
3. Run ordinary routing at 9 logical/11 physical/1712 and segmented coverage at 11 logical/12 physical/1712 only under future implementation authorization.
4. Reproduce the exact old artifacts in a clean detached `4d576e205cb20c37ba913b923a1cd39e8d800d18` worktree.
5. Invoke the producer with exactly seven operands; require three deterministic output files, exact source `63->69`, tests `1572->1712`, `+6/-0`, `+140/-0`, complete inline tuple maps, and all loss counters zero.
6. Commit P directly on S with exactly one rendered profile entry, one selector-token replacement, and allowed docs; do not include P SHA in the profile.
7. Invoke the child verifier with exactly four operands; require the exact five-node DAG, exact byte-span round trip, direct parent proof, final graph manifest, and T1 boundary.
8. Run every negative row, enforce the per-component and total script budget, and record actual SHA/count bindings without revising design expectations.
9. Obtain fresh code/rule reviews before publication. D2 remains stopped.

No green command by itself promotes the candidate. Local `ACCEPTED_1712_V1` requires the versioned manifest, exact real evidence, P verification, and required local review chain.

## 14. Rollback, stop-loss, and impacts

Rollback remains additive: revert P, then S. Never reset, amend, rebase, rewrite, mutate old profiles, or relabel the D1 candidate. Generated temporary artifacts may be deleted only after required archival/reverification.

Stop with `HUMAN_BLOCKED` for any mandatory source conflict/unavailability, dirty/wrong head, missing ancestry, old authority reproduction failure, candidate/hash drift, graph node other than the exact five, graph cycle/self-reference, noncanonical artifact, source/test delta mismatch, incomplete tuple map, nonzero removed/missing/unexpected or positive-loss count, profile rendering/span mismatch, old-profile byte change, selector fallback, P not directly parented by S, output/path/symlink violation, allowlist breach, any component or total LOC breach, workflow change beyond the frozen future one-token replacement, production/test/rule/ownership/D2 change, or a non-passing final correction review.

```text
DesignCorrectionUsed=2/2
DesignCorrectionRemaining=0
DesignCorrectionRound3=FORBIDDEN
ImplementationRepairUsed=0/2
ImplementationRepairRemaining=2
implementationAuthorized=false
workflowBoundary=DESIGN_ONLY; workflow unchanged in this correction
productionImpact=NONE
behaviorImpact=NONE
ruleImpact=NONE
roleCoverageImpact=NONE
runtimeProfileAuthority=FORBIDDEN
dynamicGraph=FORBIDDEN
dynamicSelector=FORBIDDEN
permanentMigrationFramework=FORBIDDEN
D2=FROZEN
```

A fresh independent reviewer must check the exact parent plus both corrections, rule evidence, current Governance V1.1 protocol, current registry/coverage/routing scripts, CI selector, exact Git state, graph cardinality and acyclicity, CLI arity, schemas, renderer/span rules, T1 scope, negative matrix, budget feasibility, lifecycle, and D2/workflow freeze. Valid verdicts are only `RULE_DESIGN_PASS`, `RULE_DESIGN_FIX_REQUIRED`, or `HUMAN_BLOCKED`. Any non-pass or nonempty blocker exhausts the D1.5 design correction budget and stops; no Round 3 exists. Even `RULE_DESIGN_PASS` does not itself execute implementation.

requiredNextAction=`FRESH_INDEPENDENT_RULE_DESIGN_REVIEW_ON_EXACT_CORRECTION_ROUND_2_HEAD`

READY_FOR_FRESH_INDEPENDENT_RULE_DESIGN_REVIEW_D1_5_CORRECTION_ROUND_2
