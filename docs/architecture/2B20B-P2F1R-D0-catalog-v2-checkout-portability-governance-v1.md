# Phase 3 Slice 2B20B-P2F1R-D0 Catalog V2 Checkout-Portability Governance V1

## 1. Governance identity

- `sliceId`: `2B20B-P2F1R-D0`
- `sliceName`: `Catalog V2 Checkout-Portability Test Foundation`
- `authorization`: `USER_AUTHORIZED_2B20B_P2F1R_D0_CATALOG_TEST_PORTABILITY_AND_CONDITIONAL_CE_LOCAL_CLOSURE`
- `taskType`: `TEST_INFRASTRUCTURE_FOUNDATION`
- `currentHead`: `422bd4fa5d6f31bfc6b80b6834f566fe5a1978b7`
- `branch`: `phase-3/2b20b-p2f1r-ce-c-evidence-portability-closure`
- `frozenCBehaviorHead`: `54d95e88398271e596e366cdec635a1aed82b384`
- `CEGovernanceHead`: `9dd9622ffe828137fe9341acbeaf60abfa174b25`
- `ruleEvidence`: `docs/rules/evidence/2B20B-P2F1R-D0.md`
- `ruleEvidenceSHA256`: `928d645e4f20b5e51b33ecb096f0b052cf41249988e626c9e2af6ee616046edf`
- `ruleVerdict`: `RULE_READY`
- `ruleCoverageStatus`: `SKELETON`
- `ruleSemanticsChanged`: `false`
- `roleCoverageChanged`: `false`
- `implementationAuthorized`: `false`
- `D0EvidenceRepairRound`: `0/2`

This document is a governance precheck. It is not implementation, design review,
P2F1R-D publication closure, CE evidence implementation, or acceptance.

## 2. Authorities read

The governance precheck read and reconciled:

- `AGENTS.md`;
- the ordered `project-handoff` baseline;
- `docs/agent-loop/AUTOPILOT_PROMPT.md`;
- `docs/agent-loop/REVIEW_PROTOCOL.md`;
- `docs/rules/USER_OVERRIDES.md`;
- `docs/rules/ROLE_COVERAGE_MATRIX.md`;
- `docs/rules/evidence/2B20B-P2F1R-D0.md`;
- `docs/implementation/phase-3-slice-2b20b-p2f1r-ce-failure-audit.md`;
- `docs/architecture/2B20B-P2F1R-CE-go-no-go-under-governance-v1.md`;
- the C and C1 architecture and implementation status;
- `packages/domain-core/src/domain-event-structural-schema-catalog.ts`;
- `packages/domain-core/src/domain-event-structural-schema-catalog.test.ts`;
- the checked-in generated Catalog V2;
- the actual Git object database and both observed checkout representations.

The rule-researcher independently checked all four mandatory rule sources.
D0 asserts no BOTC rule behavior and has no unresolved rule conflict.

## 3. Frozen production boundary

The following exact Git-blob SHA-256 values remain frozen:

| File | SHA-256 |
|---|---|
| `packages/domain-core/src/canonical-domain-event.ts` | `41020fbbc0cc23194c565c2b0ace5ce907942e86204e8373b29449a94b07a5b3` |
| `packages/domain-core/src/domain-event-structural-validator.ts` | `a7d7cd0294c877317ba35957f957859fda586c459aeec40a361fb8853d1531e6` |
| `packages/domain-core/src/index.ts` | `ac142d2c83a77c73aae244dc2bd3d6da9e7f01ca923fff4d22139ed10c024353` |

D0 may not change these files or any A, B, C, or C1 production file.
The diff from frozen C behavior HEAD through the governance baseline contains
only CE governance/evidence documentation and D0 rule evidence. It contains no
production behavior change.

## 4. Independently verified Catalog facts

Artifact path:
`docs/architecture/2B20B-P2F1R-C1-generated-structural-schema-catalog-v2.md`

| Fact | Verified value |
|---|---|
| `HEAD:path` blob OID | `4f9a376e56f19b241d76ce2a75be83b70859ae25` |
| Repository raw blob length | `264855` |
| Repository raw blob SHA-256 | `e0f788db370eca7ad1d1097f2a271bd9257fb5966d28081c930458d3dea85ef6` |
| Repository line endings | `626 LF`, `0 CR` |
| Generated canonical byte length | `264855` |
| Generated canonical SHA-256 | `e0f788db370eca7ad1d1097f2a271bd9257fb5966d28081c930458d3dea85ef6` |
| LF checkout SHA-256 | `e0f788db370eca7ad1d1097f2a271bd9257fb5966d28081c930458d3dea85ef6` |
| Default Windows checkout length | `265481` |
| Default Windows checkout SHA-256 | `7d912c085c61ab34d06c46d0cbfd5f3def8e10465339d608566a73eaf93763b7` |
| Default Windows line endings | `626 CRLF`, `0 lone LF` |
| First blob/default difference | byte offset `48` |
| BOM | absent from blob, generated bytes, and both checkouts |
| Difference classification | `LF_TO_CRLF_CHECKOUT_CONVERSION_ONLY` |

The generated canonical bytes and repository blob are byte-for-byte equal.
The default Windows checkout differs only because Git materializes LF blob
newlines as CRLF under the observed `core.autocrlf=true` environment.

## 5. Current failure

The affected physical test is:

- project: `domain-core`;
- file: `packages/domain-core/src/domain-event-structural-schema-catalog.test.ts`;
- ancestor path: `Catalog V2 audit projection`;
- title: `matches the checked-in frozen generated Catalog V2 path byte-for-byte`.

It currently reads working-tree UTF-8 text and compares it with the LF-only
renderer output. It therefore treats checkout representation as if it were the
checked-in repository artifact. The generator, Catalog artifact, embedded
artifact digest, AST, and accepted runtime behavior are not defective.

## 6. Required governance questions

1. Repository blob equals generated canonical bytes: `YES`; full bytes, length, SHA, newline census and BOM agree.
2. Default checkout difference is only line-ending conversion: `YES`; every LF maps to exactly one CRLF and there are no other differences.
3. Current test uses the wrong authority representation: `YES`; it reads checkout text rather than raw `HEAD:path` object bytes.
4. Correct test without C1, artifact or `.gitattributes` changes: `YES`; use fixed-path Git blob evidence.
5. Same ordinary test can pass in default and LF checkouts: `YES`; canonical comparison is checkout-independent.
6. Test can safely read the object database: `YES`; fixed `rev-parse` and `cat-file`, shell disabled, bounded output, no network/write.
7. Git object bytes become runtime authority: `NO`; they remain test-time audit evidence only.
8. Test title must change: `NO`; `HEAD:path` still denotes the checked-in path.
9. Semantic inventory changes: `NO`; suite/title/project/file/ancestor/criterion remain unchanged.
10. Full P2F1R-D must start now: `NO`; ownership, coverage, hosted evidence and publication reconciliation remain deferred.

## 7. Governance classification

D0 is a bounded test-mechanism correction to existing C1-C11 evidence. It does
not add a BOTC behavior criterion, runtime input, event, authority or semantic
test identity.

The existing C1-C11 primary identity remains:

```text
[
  "domain-core",
  "packages/domain-core/src/domain-event-structural-schema-catalog.test.ts",
  ["Catalog V2 audit projection"],
  "matches the checked-in frozen generated Catalog V2 path byte-for-byte"
]
```

Its classification remains:

- `ExpectedReachability`: `R4_FUTURE_HYPOTHETICAL_STATE`;
- `ExpectedTrust`: `T3_MODULE_PRIVATE_PURE_CORE`;
- `ExpectedPrimaryLayer`: `PURE_POLICY_SEAM`.

The persisted Git blob is validated test input. It does not create a second
primary identity or a `SUP-*` authority record.

## 8. Allowed solution

```text
fixed HEAD:path
  -> validated exact blob OID
  -> raw Git object bytes
  -> exact length and SHA-256
  -> byte-for-byte equality with generated UTF-8 bytes
```

Working-tree bytes are separately classified as:

- `MATCHES_REPOSITORY_BLOB`;
- `LF_TO_CRLF_CHECKOUT_CONVERSION_ONLY`;
- `OTHER_CHECKOUT_DIFFERENCE`.

Only the first two are valid in required clean worktrees. Classification does
not normalize or provide authority bytes.

## 9. Governance exclusions

D0 must not modify Catalog generation or content, expected digest, C1 AST, any
A/B/C/C1 production, event definitions, semantic validators, replay, batch,
snapshot, state, application, `.gitattributes`, Git configuration, workflow,
package scripts, ownership, coverage, profile, runner, role coverage, or the
protected old dirty C worktree.

D0 must not normalize CRLF before canonical equality, ignore byte differences,
fall back to worktree bytes, use a shell command string, accept an untrusted
path, change the test title, or claim complete D publication evidence.

## 10. Decision

All GO conditions are satisfied:

- required change is test-only;
- generator and artifact remain unchanged;
- `.gitattributes`, workflow and C1 production remain unchanged;
- physical identity remains unchanged;
- default Windows and LF worktrees can execute the same exact-byte assertion.

- `governanceVerdict`: `GO`
- `designAuthorized`: `true`
- `implementationAuthorized`: `false`
- `remainingGovernanceBlockers`: `[]`
- `requiredNextAction`: `MATERIALIZE_D0_DESIGN_V1_AND_REQUEST_FRESH_INDEPENDENT_RULE_DESIGN_REVIEW`

GO_FOR_D0_DESIGN_V1
