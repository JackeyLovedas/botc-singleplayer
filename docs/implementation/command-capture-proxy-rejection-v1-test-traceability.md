# Command Capture Proxy Rejection V1 Test Traceability

## Authority and scope

- Task: `COMMAND_CAPTURE_PROXY_REJECTION_V1`
- Frozen design: `docs/implementation/command-capture-proxy-rejection-v1-design.md`
- Frozen design SHA-256: `4693cddf74159c1cf310781effd74154a0d6ede8615ad873b0576dd36c68c220`
- Source commit: `ea08ddd979bc8d3e825efdf5b290bd0c3e85942f`
- Profile commit and pre-repair reviewed HEAD: `456027283f884d634ed3925d610fb0410d0d8e87`
- Repair classification: `FINAL_REVIEW_REPAIR_ROUND_1_DOCS_ONLY`
- Behavior design changed: `false`
- Rule semantics changed: `false`
- Production, test, ownership, profile tuple, workflow, dependency and topology bytes changed by this traceability repair: `false`

This document materializes the implementation-time binding required by `docs/agent-loop/REVIEW_PROTOCOL.md`. It does not replace, rewrite, or relax the frozen design. Each `CriterionId` appears exactly once in the expected table and exactly once in the actual table. Each physical identity has one primary owner here; when another criterion uses that identity, it is referenced only through a resolved supporting-authority ID.

## Frozen expected fields

The following nine fields are retained row-for-row from the frozen design.

| CriterionId | RuleClaim | CompletionCriterion | RequiredEvidenceMechanism | ExpectedReachability | ExpectedTrust | ExpectedPrimaryLayer | ExpectedResult | SupportingAuthorityRequirement |
|---|---|---|---|---|---|---|---|---|
| `C01` | External commands must not admit live Proxy data | Direct capture rejects a top-level transparent live Proxy before reflection | Call `captureSupportedCommand` on a transparent object Proxy and assert exact invalid result and zero counters | `R3` | `T1` | `STRUCTURAL_VALIDATION` | exact reason `Command values must not be Proxy objects` | `PLANNED_SUPPORTING_AUTHORITY`: official Node isProxy contract and exact-runtime probe, no mutation |
| `C02` | Validation applies recursively | Direct capture rejects a Proxy in a plain nested own data property | Plain envelope with nested Proxy; exact invalid result; target unchanged; zero traps | `R3` | `T1` | `STRUCTURAL_VALIDATION` | invalid with exact Proxy reason | `C01` may support shared guard ordering; no separate accepted producer |
| `C03` | Arrays cannot bypass the Proxy boundary | Direct capture rejects a Proxy-wrapped array before `Array.isArray`/descriptor work | Plain envelope containing Proxy array; count traps; assert exact invalid | `R3` | `T1` | `STRUCTURAL_VALIDATION` | invalid; all traps `0` | include plain-array positive regression as support |
| `C04` | Revocation cannot force reflective probing | Direct capture rejects an already revoked Proxy without throwing from a trap | `Proxy.revocable`, revoke, capture, assert exact invalid | `R3` | `T1` | `STRUCTURAL_VALIDATION` | invalid; no trap/TypeError escapes capture | preserve the existing revoked-command test unchanged as support |
| `C05` | Prototype reflection is forbidden before Proxy rejection | `getPrototypeOf` trap is never invoked for object/array/null-target/throwing Proxies | Dedicated counter with throwing trap; include null-prototype target | `R3` | `T1` | `STRUCTURAL_VALIDATION` | count `0`; exact invalid reason | exact Node 24.15.0 probe support |
| `C06` | Own-key reflection is forbidden before Proxy rejection | `ownKeys` trap is never invoked | Dedicated throwing/counted `ownKeys` Proxy capture | `R3` | `T1` | `STRUCTURAL_VALIDATION` | count `0`; exact invalid reason | no reflection-after-catch implementation permitted |
| `C07` | Descriptor reflection is forbidden before Proxy rejection | `getOwnPropertyDescriptor` trap is never invoked, including a descriptor-changing Proxy | Throwing proxy plus a nonthrowing proxy that changes descriptor answers; count | `R3` | `T1` | `STRUCTURAL_VALIDATION` | count `0`; both invalid | stored-fingerprint changing-Proxy regression is supporting only |
| `C08` | Property reads are forbidden before Proxy rejection | `get` trap is never invoked, including a transparent/nonthrowing Proxy | Throwing and nonthrowing counted `get` handlers | `R3` | `T1` | `STRUCTURAL_VALIDATION` | count `0`; both invalid | no target access or property equality read |
| `C09` | Formal execute must not re-reflect a top-level Proxy | `execute` rejects top-level live/revoked/throwing Proxy through existing TypeError boundary with every trap count zero | Invoke real `GameApplicationService.execute`; assert rejected promise and exact TypeError text; no test-side property read | `R1` | `T1` | `APPLICATION_COMMAND_INTEGRATION` | exact own-data-property `TypeError`; all trap counts `0` | `C01,C04–C08` direct capture authorities support guard behavior |
| `C10` | Invalid top-level Proxy must stop before persistence | Formal top-level Proxy execution performs zero receipt/event reads and zero writes | Inject an auditing store with four independent call counters and inspect counters without calling store ports | `R1` | `T1` | `APPLICATION_COMMAND_INTEGRATION` | all four port counts `0`; no receipt/event/result object | `PLANNED_SUPPORTING_AUTHORITY`: fresh empty store, accepted status `NONE`, mutation `NONE` |
| `C11` | A trusted plain gameId may classify a nested capture failure | Plain top-level command with nested Proxy returns the exact existing command-validation failure | Real `execute` with own data `gameId`, nested counted Proxy, assert entire result shape and absent current version | `R1` | `T1` | `APPLICATION_COMMAND_INTEGRATION` | `failed / DependencyExecutionFailed / command-validation / retryable:true`; exact message; traps `0` | direct `C02` authority supports capture reason |
| `C12` | Nested Proxy failure precedes persistence reads | The C11 path performs zero receipt and event reads | Auditing store counters on a separate formal service authority | `R1` | `T1` | `APPLICATION_COMMAND_INTEGRATION` | `findCommandReceipt=0`, `loadDomainEvents=0` | same uncommitted empty-store support pattern as C10 |
| `C13` | Retryable capture failure never consumes a receipt | The C11 path performs zero accepted/rejected receipt writes and receipt count remains zero | Auditing store write counters and side-effect-free count field | `R1` | `T1` | `APPLICATION_COMMAND_INTEGRATION` | both write counters `0`; zero receipt | no call to `findCommandReceipt` after execution when proving zero-read count |
| `C14` | Retryable capture failure never appends events | The C11 path performs zero accepted commits and leaves zero events/game version unchanged | Auditing store accepted-commit counter and direct test-owned initial state | `R1` | `T1` | `APPLICATION_COMMAND_INTEGRATION` | zero event writes/events; version remains initial | no rebuild or forged event support |
| `C15` | Existing revoked incoming-command coverage remains valid | The pre-existing revoked Proxy structural test continues to pass without weakened assertions | Run unchanged existing test together with new exact-reason authority | `R3` | `T1` | `STRUCTURAL_VALIDATION` | valid=false; no regression | existing physical test is the authority; do not rename/remove/weaken it |
| `C16` | Stored fingerprint Proxy defenses remain independent and fail closed | Existing live/revoked/throwing/changing stored fingerprint Proxy tests remain exact | Run `validateCommandFingerprint`, equality and service stored-receipt Proxy authorities unchanged | `R3` | `T1` | `STRUCTURAL_VALIDATION` | false/conflict; stored Proxy property-read counters `0` | existing service conflict tests are supporting because main row is direct validation |
| `C17` | Canonical bytes and digest are immutable for legal data | Existing tagged-tree UTF-8/SHA-256 golden vector stays byte-identical | Assert exact canonical string, byte length `111`, digest `8baf95...542`, six-key shape | `R1` | `T1` | `STRUCTURAL_VALIDATION` | exact existing vector unchanged | existing golden test is authoritative and must not be weakened |
| `C18` | Legal object key insertion order does not affect identity | Reordered plain objects retain exact canonical JSON and service idempotency | Direct fingerprint equality plus existing real service reordered-command test | `R1` | `T1` | `APPLICATION_COMMAND_INTEGRATION` | second service result equals first with `idempotent:true`; one receipt/event batch | direct fingerprint unit is supporting only |
| `C19` | Existing receipts continue matching legal incoming commands | A fingerprinted receipt created before retry still matches the same/reordered plain command with no migration or duplicate write | Real service accepts plain command, captures stored receipt, retries equivalent plain command, re-reads exact same receipt | `R1` | `T1` | `APPLICATION_COMMAND_INTEGRATION` | idempotent accepted result; one receipt; unchanged fingerprint bytes; no extra events | fresh accepted receipt is `ACCEPTED`, mutation `NONE`; legacy malformed receipt tests stay supporting |
| `C20` | Schema/version/algorithm identity is frozen | The three exported constants equal exact authorized literals and no public shape/version changes | Literal assertions plus source diff audit against the three constant declarations | `R1` | `T1` | `STRUCTURAL_VALIDATION` | exact schema, canonicalization and `SHA-256` literals | C17 golden and final production allowlist/diff audit support |

## Actual implementation binding

`ActualTestTitle` is the complete current Vitest physical identity. C15 is the exact generated physical title formed by the source row `revoked proxy` and source template `rejects unsupported command structure: %s`.

| CriterionId | ActualTestFile | ActualTestTitle | ActualPrimaryLayer | ActualReachability | ActualTrust | SupportingAuthorityId | MainAssertion | ProductionEntry | FaultMechanism | MechanismMatch | Explanation |
|---|---|---|---|---|---|---|---|---|---|---|---|
| `C01` | `packages/application/src/command-fingerprint.test.ts` | `C01 rejects a transparent top-level object Proxy before every installed object trap` | `STRUCTURAL_VALIDATION` | `R3` | `T1` | `SUP-CCPRV1-001` | Exact invalid reason and every installed object-trap counter is zero | `captureSupportedCommand` | Transparent live object Proxy with all 11 object traps counted | `PASS` | Direct T1 structural validation proves rejection precedes reflection; exact-runtime Node authority resolves the feasibility support. |
| `C02` | `packages/application/src/command-fingerprint.test.ts` | `C02 rejects a nested object Proxy before every installed object trap` | `STRUCTURAL_VALIDATION` | `R3` | `T1` | `SUP-CCPRV1-002` | Exact invalid reason, nested target unchanged, every installed trap counter zero | `captureSupportedCommand` | Proxy in a plain envelope own data property | `PASS` | The primary proves recursive rejection; C01 is supporting guard-order authority only. |
| `C03` | `packages/application/src/command-fingerprint.test.ts` | `C03 rejects a Proxy-wrapped array before Array.isArray, descriptors, or traps` | `STRUCTURAL_VALIDATION` | `R3` | `T1` | `SUP-CCPRV1-003` | Proxy array is invalid with zero traps while the plain dense array remains valid | `captureSupportedCommand` | Proxy-wrapped dense array plus plain-array positive control | `PASS` | The same physical authority contains the required positive control; the support registry identifies that accepted control without creating another primary owner. |
| `C04` | `packages/application/src/command-fingerprint.test.ts` | `C04 rejects a revoked Proxy with the exact reason and without invoking its handler` | `STRUCTURAL_VALIDATION` | `R3` | `T1` | `SUP-CCPRV1-004` | Revoked Proxy returns the exact invalid reason without an escaped revocation error and with zero traps | `captureSupportedCommand` | Already-revoked `Proxy.revocable` value | `PASS` | The new exact-reason authority is primary; the unchanged pre-existing revoked-command case remains support. |
| `C05` | `packages/application/src/command-fingerprint.test.ts` | `C05 rejects null-target and throwing-prototype Proxies before getPrototypeOf` | `STRUCTURAL_VALIDATION` | `R3` | `T1` | `SUP-CCPRV1-001` | Null-prototype target and throwing `getPrototypeOf` Proxy both return exact invalid reason with all counters zero | `captureSupportedCommand` | Null-prototype target Proxy and throwing prototype trap | `PASS` | Direct trap counters prove the frozen ordering and the exact-runtime Node probe supplies independent support. |
| `C06` | `packages/application/src/command-fingerprint.test.ts` | `C06 rejects an ownKeys-throwing Proxy before key reflection` | `STRUCTURAL_VALIDATION` | `R3` | `T1` | `NONE` | Exact invalid reason and `ownKeys` plus all other trap counters remain zero | `captureSupportedCommand` | Throwing counted `ownKeys` trap | `PASS` | The primary directly proves no key reflection and no reflection-after-catch path; no separate supporting authority is required. |
| `C07` | `packages/application/src/command-fingerprint.test.ts` | `C07 rejects throwing and descriptor-changing Proxies before descriptor reflection` | `STRUCTURAL_VALIDATION` | `R3` | `T1` | `SUP-CCPRV1-005` | Throwing and answer-changing descriptor Proxies are both exact-invalid with descriptor counters zero | `captureSupportedCommand` | Throwing and nonthrowing `getOwnPropertyDescriptor` traps | `PASS` | The primary proves incoming capture; the independent stored-fingerprint changing-Proxy regression remains support only. |
| `C08` | `packages/application/src/command-fingerprint.test.ts` | `C08 rejects throwing and transparent Proxies before property reads` | `STRUCTURAL_VALIDATION` | `R3` | `T1` | `NONE` | Throwing and transparent `get` Proxies are both exact-invalid with every counter zero | `captureSupportedCommand` | Throwing and nonthrowing counted `get` handlers | `PASS` | The primary directly proves no target access or property equality read; no separate support is required. |
| `C09` | `packages/application/src/game-application-service.test.ts` | `C09 rejects top-level live, revoked, and throwing Proxies without traps` | `APPLICATION_COMMAND_INTEGRATION` | `R1` | `T1` | `SUP-CCPRV1-002; SUP-CCPRV1-006; SUP-CCPRV1-007; SUP-CCPRV1-008; SUP-CCPRV1-009; SUP-CCPRV1-010` | Real execute rejects each Proxy with exact TypeError text and all installed trap counters zero | `GameApplicationService.execute` | Live, revoked and throwing top-level command Proxies at the formal command boundary | `PASS` | Formal-entry rejection is primary; direct C01/C04-C08 capture authorities support the shared guard ordering. |
| `C10` | `packages/application/src/game-application-service.test.ts` | `C10 stops a top-level Proxy before every command-store port` | `APPLICATION_COMMAND_INTEGRATION` | `R1` | `T1` | `SUP-CCPRV1-011` | Receipt reads, event reads, accepted writes, rejected writes, receipt count and game version all remain zero | `GameApplicationService.execute` | Top-level Proxy against a fresh audited empty store | `PASS` | Four independent port counters prove the persistence stop; the accepted empty-store fixture resolves once as support. |
| `C11` | `packages/application/src/game-application-service.test.ts` | `C11 returns the exact retryable command-validation failure for a nested Proxy` | `APPLICATION_COMMAND_INTEGRATION` | `R1` | `T1` | `SUP-CCPRV1-012` | Entire retryable failure object is exact, `currentGameVersion` is absent, traps and receipts remain zero | `GameApplicationService.execute` | Plain command with trusted own data `gameId` and nested counted Proxy | `PASS` | The formal application failure is primary; C02 supplies only the direct capture-reason support. |
| `C12` | `packages/application/src/game-application-service.test.ts` | `C12 performs zero receipt and event reads for a nested Proxy failure` | `APPLICATION_COMMAND_INTEGRATION` | `R1` | `T1` | `SUP-CCPRV1-011` | `receiptReadCount` and `eventReadCount` are exactly zero and all Proxy traps remain zero | `GameApplicationService.execute` | Nested Proxy against the accepted audited empty store | `PASS` | Separate formal authority proves capture failure precedes both persistence reads. |
| `C13` | `packages/application/src/game-application-service.test.ts` | `C13 performs zero accepted or rejected receipt writes for a nested Proxy failure` | `APPLICATION_COMMAND_INTEGRATION` | `R1` | `T1` | `NONE` | Accepted and rejected receipt write counters and side-effect-free receipt count are exactly zero | `GameApplicationService.execute` | Nested Proxy failure with audited receipt writes | `PASS` | The count field proves no receipt consumption without an after-execution receipt lookup; no separate support is required. |
| `C14` | `packages/application/src/game-application-service.test.ts` | `C14 performs zero event writes and leaves the initial game version unchanged` | `APPLICATION_COMMAND_INTEGRATION` | `R1` | `T1` | `NONE` | Accepted commit count and event reads remain zero and the test-owned initial version remains zero | `GameApplicationService.execute` | Nested Proxy failure before event commit | `PASS` | The formal path proves no append and no version mutation without rebuild or forged-event support. |
| `C15` | `packages/application/src/command-fingerprint.test.ts` | `rejects unsupported command structure: revoked proxy` | `STRUCTURAL_VALIDATION` | `R3` | `T1` | `SUP-CCPRV1-006` | Unchanged pre-existing generated case still returns an object matching `{ valid: false }` | `captureSupportedCommand` | Revoked Proxy row in the existing unsupported-structure table | `PASS` | The pre-existing physical identity remains the primary regression and C04 supplies the new exact-reason support; no assertion was renamed or weakened. |
| `C16` | `packages/application/src/command-fingerprint.test.ts` | `rejects every stored fingerprint Proxy before revoked, reflection, property, or late traps can run` | `STRUCTURAL_VALIDATION` | `R3` | `T1` | `SUP-CCPRV1-013; SUP-CCPRV1-014` | Validation and equality never throw, return false for every stored Proxy, and the late canonical-read counter remains zero | `validateCommandFingerprint`; `commandFingerprintsRepresentSameCommand` | Live, revoked, throwing, transparent and late-changing persisted fingerprint Proxies | `PASS` | Direct stored-fingerprint validation is primary; the two real service conflict authorities remain support only. |
| `C17` | `packages/application/src/command-fingerprint.test.ts` | `uses the exact tagged canonical tree, six-key fingerprint, UTF-8 length, and SHA-256 vector` | `STRUCTURAL_VALIDATION` | `R1` | `T1` | `NONE` | Exact canonical string, six-key shape, UTF-8 byte length 111 and digest `8baf95bb080ffb729c9ae6abe58ab64370ead5943e8e27b2297519a9ce7ed542` remain unchanged | `captureSupportedCommand`; `validateCommandFingerprint` | Legal plain canonical data golden vector | `PASS` | The existing authoritative golden test directly proves the frozen legal-data representation and digest. |
| `C18` | `packages/application/src/command-fingerprint.test.ts` | `sorts object keys by code unit, preserves array order, and distinguishes absent from own undefined` | `STRUCTURAL_VALIDATION` | `R1` | `T1` | `SUP-CCPRV1-015` | Direct fingerprints for reordered plain objects have identical canonical JSON and represent the same command | `captureSupportedCommand`; `commandFingerprintsRepresentSameCommand` | Legal plain objects with reordered own keys | `PASS` | `TRACEABILITY_CORRECTION`: the frozen `ExpectedPrimaryLayer=APPLICATION_COMMAND_INTEGRATION` is retained. To obey the one-primary-owner rule, this direct identity is the C18 primary and the real service identity is supporting here and primary only for C19. The unchanged `RequiredEvidenceMechanism` is fully proved by this direct equality authority plus `SUP-CCPRV1-015`, which proves service idempotency, one receipt and one event batch. Behavior design and rule semantics remain unchanged. |
| `C19` | `packages/application/src/game-application-service.test.ts` | `treats reordered own data properties as the same structural command` | `APPLICATION_COMMAND_INTEGRATION` | `R1` | `T1` | `SUP-CCPRV1-016; SUP-CCPRV1-017` | First execution is accepted, reordered retry is the exact idempotent result, one event and receipt remain, and the re-read receipt is exactly the captured original | `GameApplicationService.execute` | Accepted plain command followed by the same command with reordered own data properties | `PASS` | This criterion exclusively owns the service physical identity; the accepted receipt producer and unchanged legacy malformed-receipt regression resolve as support. |
| `C20` | `packages/application/src/command-fingerprint.test.ts` | `C20 freezes the exact fingerprint schema, canonicalization, and digest literals` | `STRUCTURAL_VALIDATION` | `R1` | `T1` | `SUP-CCPRV1-018; SUP-CCPRV1-019` | All three exported constants equal the exact frozen string literals | exported command-fingerprint constants | Literal identity regression plus production-source diff boundary | `PASS` | Literal assertions are primary; the C17 golden vector and exact source diff audit prove no public shape/version change. |

## Supporting authority registry

Each `SupportingAuthorityId` below is defined exactly once. Supporting authorities do not own a primary layer and do not replace the primary test.

| SupportingAuthorityId | Producer | SourceTestOrFixture | AuthorityStatus | UsedByCriteria | MutationDisposition |
|---|---|---|---|---|---|
| `SUP-CCPRV1-001` | Node.js `util.types.isProxy` and exact Node `24.15.0` runtime probe | `docs/implementation/command-capture-proxy-rejection-v1-design-review.md`, `runtimeProbe` | `ACCEPTED` | `C01,C05` | `NONE` |
| `SUP-CCPRV1-002` | Direct incoming-command Proxy guard authority | `packages/application/src/command-fingerprint.test.ts` — `C01 rejects a transparent top-level object Proxy before every installed object trap` | `HOSTILE` | `C02,C09` | `NONE` |
| `SUP-CCPRV1-003` | Plain dense-array positive control inside the C03 authority | `packages/application/src/command-fingerprint.test.ts` — `C03 rejects a Proxy-wrapped array before Array.isArray, descriptors, or traps` | `ACCEPTED` | `C03` | `NONE` |
| `SUP-CCPRV1-004` | Pre-existing revoked incoming-command regression | `packages/application/src/command-fingerprint.test.ts` — `rejects unsupported command structure: revoked proxy` | `HOSTILE` | `C04` | `NONE` |
| `SUP-CCPRV1-005` | Stored-fingerprint descriptor/equality changing-Proxy regression | `packages/application/src/command-fingerprint.test.ts` — `rejects a nonthrowing Proxy that swaps canonical A for canonical B on the final equality read` | `HOSTILE` | `C07` | `NONE` |
| `SUP-CCPRV1-006` | Exact revoked incoming-command capture authority | `packages/application/src/command-fingerprint.test.ts` — `C04 rejects a revoked Proxy with the exact reason and without invoking its handler` | `HOSTILE` | `C09,C15` | `NONE` |
| `SUP-CCPRV1-007` | Exact prototype-trap capture authority | `packages/application/src/command-fingerprint.test.ts` — `C05 rejects null-target and throwing-prototype Proxies before getPrototypeOf` | `HOSTILE` | `C09` | `NONE` |
| `SUP-CCPRV1-008` | Exact own-key-trap capture authority | `packages/application/src/command-fingerprint.test.ts` — `C06 rejects an ownKeys-throwing Proxy before key reflection` | `HOSTILE` | `C09` | `NONE` |
| `SUP-CCPRV1-009` | Exact descriptor-trap capture authority | `packages/application/src/command-fingerprint.test.ts` — `C07 rejects throwing and descriptor-changing Proxies before descriptor reflection` | `HOSTILE` | `C09` | `NONE` |
| `SUP-CCPRV1-010` | Exact property-read-trap capture authority | `packages/application/src/command-fingerprint.test.ts` — `C08 rejects throwing and transparent Proxies before property reads` | `HOSTILE` | `C09` | `NONE` |
| `SUP-CCPRV1-011` | Fresh `AuditingCommandStore` with zero initial receipts, events and version | `packages/application/src/game-application-service.test.ts` — `C10 stops a top-level Proxy before every command-store port` | `ACCEPTED` | `C10,C12` | `NONE` |
| `SUP-CCPRV1-012` | Direct nested incoming-command Proxy authority | `packages/application/src/command-fingerprint.test.ts` — `C02 rejects a nested object Proxy before every installed object trap` | `HOSTILE` | `C11` | `NONE` |
| `SUP-CCPRV1-013` | Revoked stored-fingerprint real-service conflict authority | `packages/application/src/game-application-service.test.ts` — `resolves an exact nonpersisted idempotency conflict for a revoked stored fingerprint proxy` | `HOSTILE` | `C16` | `PERSISTED_OR_IMPORTED_MUTATED` |
| `SUP-CCPRV1-014` | Changing stored-fingerprint real-service conflict authority | `packages/application/src/game-application-service.test.ts` — `rejects a stored fingerprint Proxy that swaps command A to incoming command B on the equality read` | `HOSTILE` | `C16` | `PERSISTED_OR_IMPORTED_MUTATED` |
| `SUP-CCPRV1-015` | Real service reordered-command idempotency authority | `packages/application/src/game-application-service.test.ts` — `treats reordered own data properties as the same structural command` | `ACCEPTED` | `C18` | `NONE` |
| `SUP-CCPRV1-016` | Fresh accepted receipt produced and captured by the first C19 execution | `packages/application/src/game-application-service.test.ts` — `treats reordered own data properties as the same structural command` | `ACCEPTED` | `C19` | `NONE` |
| `SUP-CCPRV1-017` | Legacy and malformed stored-fingerprint conflict regression | `packages/application/src/game-application-service.test.ts` — `fails closed for legacy, malformed, and digest-only-matching stored command fingerprints` | `LEGACY` | `C19` | `PERSISTED_OR_IMPORTED_MUTATED` |
| `SUP-CCPRV1-018` | Exact legal-data fingerprint golden authority | `packages/application/src/command-fingerprint.test.ts` — `uses the exact tagged canonical tree, six-key fingerprint, UTF-8 length, and SHA-256 vector` | `ACCEPTED` | `C20` | `NONE` |
| `SUP-CCPRV1-019` | Frozen source-commit production diff audit | `git diff c0c0cdfef1c1aa4cebb841f9867007a319701459..ea08ddd979bc8d3e825efdf5b290bd0c3e85942f -- packages/application/src/command-fingerprint.ts packages/application/src/game-application-service.ts` | `ACCEPTED` | `C20` | `NONE` |

## Mechanical validation contract

The docs-only repair is valid only when a read-only mechanical check proves all of the following:

1. Expected and actual tables each contain `C01` through `C20` exactly once and no other criterion.
2. Every one of the nine frozen expected fields is byte-for-byte equal to the corresponding frozen design table cell.
3. Every actual row contains `ActualTestFile`, `ActualTestTitle`, `ActualPrimaryLayer`, `ActualReachability`, `ActualTrust`, `SupportingAuthorityId`, `MainAssertion`, `ProductionEntry`, `FaultMechanism`, `MechanismMatch`, and `Explanation`.
4. Every actual test title resolves to its named current test source; the C15 generated title resolves from its table row plus template.
5. Every actual primary layer belongs to the eight protocol layers, reachability belongs to `R1`–`R4`, trust belongs to `T1`–`T3`, and `MechanismMatch` is `PASS` only where the recorded primary-plus-support mechanism proves the frozen completion criterion.
6. Each `ActualTestFile + ActualTestTitle` physical identity has exactly one primary owner.
7. Every non-`NONE` `SUP-CCPRV1-NNN` reference resolves to exactly one registry row; every registry row is referenced and its `UsedByCriteria` equals its actual consumers.
8. C18 retains the frozen expected application layer, records the actual structural layer, and names the unchanged primary-plus-support evidence mechanism instead of silently changing the design.

TRACEABILITY_READY_FOR_DOCS_ONLY_REPAIR_REVIEW
